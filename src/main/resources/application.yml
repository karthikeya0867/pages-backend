spring:
  application:
    name: Pages-Backend
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: sai@2006
    url: jdbc:postgresql://localhost:5432/pagesdb
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  main:
    banner-mode: off
  security:
    user:
      password: aaaa
server:
  servlet:
    context-path: /api

logging:
  level:
     org:
       springframework:
         security: trace
